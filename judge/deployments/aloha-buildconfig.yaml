apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: aloha
  namespace: judge
spec:
  output:
    to:
      kind: ImageStreamTag
      name: aloha:latest
  source:
    contextDir: /judge/aloha
    git:
      uri: git@github.com:catalyst-ctfd/catalyst.git
    sourceSecret:
      name: repo-key
    type: Git
  strategy:
    sourceStrategy:
      from:
        kind: ImageStreamTag
        name: python:3.9-ubi8
        namespace: openshift
    type: Source
  triggers:
    - generic:
        secretReference:
          name: aloha-generic-webhook-secret
      type: Generic
    - github:
        secretReference:
          name: aloha-github-webhook-secret
      type: GitHub
    - imageChange: {}
      type: ImageChange
    - type: ConfigChange
